{
  "devDependencies": {
    "@biomejs/biome": "^2.5.11",
    "stylelint": "^17.14.1",
    "stylelint-config-standard": "^40.0.0"
  },
  "private": true,
  "scripts": {
    "lint": "bun run lint:css && bun run lint:js && hugo --source exampleSite --themesDir ../.. --theme hugo-wishingwell-theme --gc --minify --cleanDestinationDir --panicOnWarning --printPathWarnings --noBuildLock",
    "lint:css": "stylelint \"assets/css/**/*.css\"",
    "lint:js": "biome lint assets/js/",
    "lint:fix": "stylelint --fix \"assets/css/**/*.css\" && biome check --write assets/js/"
  },
  "type": "module"
}
